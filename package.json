{
  "name": "lattice",
  "version": "0.5.0",
  "description": "Moderní, štíhlá a framework-agnostická datová tabulka (grid) ve vanilla JS (ESM). Vlastní implementace s jedním zdrojem pravdy pro persistenci a deterministickým initem.",
  "type": "module",
  "main": "src/index.js",
  "module": "src/index.js",
  "unpkg": "dist/lattice.min.js",
  "jsdelivr": "dist/lattice.min.js",
  "exports": {
    ".": "./src/index.js",
    "./bundle": "./dist/lattice.js",
    "./css": "./src/lattice.css"
  },
  "files": [
    "src",
    "dist",
    "docs"
  ],
  "scripts": {
    "test": "node --test",
    "demo": "node demo/server.js",
    "build": "esbuild src/index.js --bundle --format=esm --outfile=dist/lattice.js && esbuild src/index.js --bundle --format=esm --minify --outfile=dist/lattice.min.js && cp src/lattice.css dist/lattice.css"
  },
  "keywords": [
    "grid",
    "table",
    "datagrid",
    "datatable",
    "vanilla-js",
    "esm"
  ],
  "license": "MIT",
  "sideEffects": [
    "*.css"
  ],
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
