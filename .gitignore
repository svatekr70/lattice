node_modules/
*.log
.DS_Store
.env

# lokální referenční materiály ke čtení, nepatří do repa
reference/

# lokální nástroje / interní zadání — nepatří do repa
.claude/
nova-grid-komponenta-zadani.md
/data/

# Depka deploy (tajnosti + lokální stav)
deploy/deploy.neon
deploy/checksums_*.json
